import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  text: {
    flex: 1,
    fontSize: 16,
    marginTop: 40,
    color: '#777',
    textAlign: "center"
  },

  textBold: {
    fontWeight: 'bold',
    color: '#000'
  },

  button: {
    marginTop: 30,
    padding: 15
  },

  buttonText: {
    color: '#0095DA'
  }
});

export default styles;